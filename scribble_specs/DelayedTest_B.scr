module src.com.simonjf.scribbletest.DelayedTest_B;

local protocol DelayedTest at B(role A,role B, transient role C) {
	X() from A;
	
	invitation for C {
		Y() to C;
		Z() from C;
	}
	
	invitation for C {
		Y() to C;
		Z() from C;
	}
}
